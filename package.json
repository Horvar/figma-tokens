{
  "name": "figma-tokens",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tokens:prepare": "node flattenTokens.cjs",
    "tokens:build": "npm run tokens:prepare && style-dictionary build --config ./style-dictionary.config.json --verbose"
  },
  "devDependencies": {
    "style-dictionary": "^4.3.2",
    "vite": "^6.0.5"
  }
}
